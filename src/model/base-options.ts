export class BaseOptions {
  apiKey: string;
  resourceGroup: string;
  region: string;
  registry?: string;
  namespace?: string;
  imageName: string;
  imageVersion: string;
  buildNumber?: string;
}
