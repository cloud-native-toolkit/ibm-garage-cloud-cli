export class BaseOptions {
  apiKey: string;
  resourceGroup: string;
  region: string;
  registry?: string;
  registryNamespace?: string;
  imageName?: string;
  imageVersion?: string;
  buildNumber?: string;
}
